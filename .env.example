# TimescaleDB / PostgreSQL connection
POSTGRES_USER=marketdata
POSTGRES_PASSWORD=changeme
POSTGRES_DB=marketdata
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Retention policies (days). Set to 0 to disable.
TICK_RETENTION_DAYS=90
ORDERBOOK_RETENTION_DAYS=30
OHLCV_RETENTION_DAYS=0
